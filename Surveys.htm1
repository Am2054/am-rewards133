<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>استبيانات - Am Rewards</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-auth-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-firestore-compat.js"></script>
</head>
<body class="bg-gray-100 text-right px-4 py-6">

  <div class="max-w-xl mx-auto bg-white p-6 rounded-xl shadow">
    <h2 class="text-xl font-bold mb-4">📊 أكمل استبيانات واحصل على نقاط</h2>

    <div class="space-y-4">
      <div class="p-4 border rounded">
        <p class="font-semibold">CPX Research</p>
        <a href="https://www.cpx-research.com/" target="_blank" class="text-blue-500 underline">فتح الاستبيان</a>
        <button onclick="completeTask('survey_cpx')" class="mt-2 bg-green-500 text-white px-3 py-1 rounded">أنهيته</button>
      </div>

      <div class="p-4 border rounded">
        <p class="font-semibold">BitLabs</p>
        <a href="https://www.bitlabs.ai/" target="_blank" class="text-blue-500 underline">فتح الاستبيان</a>
        <button onclick="completeTask('survey_bitlabs')" class="mt-2 bg-green-500 text-white px-3 py-1 rounded">أنهيته</button>
      </div>

      <div class="p-4 border rounded">
        <p class="font-semibold">TheoremReach</p>
        <a href="https://www.theoremreach.com/" target="_blank" class="text-blue-500 underline">فتح الاستبيان</a>
        <button onclick="completeTask('survey_theorem')" class="mt-2 bg-green-500 text-white px-3 py-1 rounded">أنهيته</button>
      </div>
    </div>
  </div>

  <script>
    const firebaseConfig = {
      // 🔐 firebaseConfig هنا
    };
    firebase.initializeApp(firebaseConfig);
    const db = firebase.firestore();
    const auth = firebase.auth();

    function completeTask(taskId) {
      auth.onAuthStateChanged(user => {
        if (user) {
          const userRef = db.collection("users").doc(user.uid);
          const taskPath = `tasks.${taskId}`;
          userRef.update({
            points: firebase.firestore.FieldValue.increment(80),
            [taskPath]: true
          }).then(() => {
            alert("✅ تم احتساب 80 نقطة");
          });
        }
      });
    }
  </script>
</body>
</html>
