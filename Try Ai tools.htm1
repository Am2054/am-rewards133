<!DOCTYPE html>
<html lang="ar">
<head>
  <meta charset="UTF-8">
  <title>تجربة أدوات AI - Am Rewards</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-app-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-auth-compat.js"></script>
  <script src="https://www.gstatic.com/firebasejs/9.6.1/firebase-firestore-compat.js"></script>
</head>
<body class="bg-gray-100 text-right px-4 py-6">

  <div class="max-w-lg mx-auto bg-white p-6 rounded-xl shadow">
    <h2 class="text-xl font-bold mb-4">🤖 جرّب أدوات الذكاء الاصطناعي واحصل على نقاط</h2>

    <div class="space-y-4">
      <div class="p-4 border rounded">
        <p class="font-semibold">ChatGPT</p>
        <a href="https://chat.openai.com" target="_blank" class="text-blue-500 underline">فتح الأداة</a>
        <button onclick="completeTask('ai_chatgpt')" class="mt-2 bg-green-500 text-white px-3 py-1 rounded">جربتها</button>
      </div>

      <div class="p-4 border rounded">
        <p class="font-semibold">Remove.bg</p>
        <a href="https://www.remove.bg" target="_blank" class="text-blue-500 underline">فتح الأداة</a>
        <button onclick="completeTask('ai_removebg')" class="mt-2 bg-green-500 text-white px-3 py-1 rounded">جربتها</button>
      </div>

      <div class="p-4 border rounded">
        <p class="font-semibold">Canva AI</p>
        <a href="https://www.canva.com/ai/" target="_blank" class="text-blue-500 underline">فتح الأداة</a>
        <button onclick="completeTask('ai_canva')" class="mt-2 bg-green-500 text-white px-3 py-1 rounded">جربتها</button>
      </div>
    </div>
  </div>

  <script>
    const firebaseConfig = {
      // 🔐 firebaseConfig هنا
    };
    firebase.initializeApp(firebaseConfig);
    const db = firebase.firestore();
    const auth = firebase.auth();

    function completeTask(taskId) {
      auth.onAuthStateChanged(user => {
        if (user) {
          const userRef = db.collection("users").doc(user.uid);
          const taskPath = `tasks.${taskId}`;
          userRef.update({
            points: firebase.firestore.FieldValue.increment(30),
            [taskPath]: true
          }).then(() => {
            alert("✅ تم احتساب 30 نقطة");
          });
        }
      });
    }
  </script>
</body>
</html>
